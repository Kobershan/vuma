<Project Sdk="Microsoft.NET.Sdk">

  <!--
    CLAUDE.md §7 rule 1: Domain references NOTHING. Not Application, not
    Infrastructure, not a NuGet package that drags in a framework. If you are
    about to add a reference here, the design is wrong, not this file.
    Enforced by ZenithRetail.ArchitectureTests.
  -->

  <PropertyGroup>
    <RootNamespace>ZenithRetail.Domain</RootNamespace>
  </PropertyGroup>

</Project>
