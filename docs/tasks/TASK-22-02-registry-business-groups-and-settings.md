# TASK-22-02 — Business groups, settings and append-only registry audit

**Depends on:** 22-01

Add business groups, transfer threshold, costing/discrepancy defaults, SLA calendar, store-code prefix and immutable old/new audit records. Test tenant isolation and defaults.

**Acceptance:** implementation follows ADR-099 and ADR-116, uses the existing registry/company context seams, preserves append-only audit and includes focused domain/application/integration tests.

