# TASK-25-003 — Leave management

Adds leave requests with pending/approved/rejected lifecycle and clock-supplied decision timestamps.
All records remain tenant filtered and use `hr_management.leave_requests`.
