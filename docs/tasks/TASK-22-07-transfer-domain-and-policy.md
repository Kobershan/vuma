# TASK-22-07 — Transfer request domain and policy decisions

**Depends on:** 22-02, 22-03, 22-04

Implement append-only transfer state machine, threshold routing, holding-company semantics, SLA policy, partial remainder and reverse reference.

**Acceptance:** implementation follows ADR-099 and ADR-116, uses the existing registry/company context seams, preserves append-only audit and includes focused domain/application/integration tests.

